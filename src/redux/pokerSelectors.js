export const stageSelector = (state) => state.stage;
export const playerSelector = (state) => state.player;
export const opponentSelector = (state) => state.opponent;
export const tableSelector = (state) => state.table;
